services:
  - type: web
    name: universal-booking-system
    env: node
    rootDir: new_system/services/booking_service
    # This unified command builds the static frontend, then installs backend dependencies!
    buildCommand: cd ../../frontend && npm install && npm run build && cd ../services/booking_service && npm install
    startCommand: node server.js
